require File.expand_path(File.join(File.dirname(__FILE__),'..','spec_helper'))

module SSLTest
  describe SSLTestCase do
    describe "#run" do
      # it loads a config
      # it launches the test runner
      # it sets up a callback for grabbing the result
      # it returns the result when it finishes
    end
  end
end


